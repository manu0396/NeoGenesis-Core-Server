1. Ensure your PostgreSQL host/credentials match `values.yaml` (`postgres.host`, `postgres.user`, `postgres.password`).
2. The chart exposes the server on port `{{ .Values.service.port }}` by default (ClusterIP `{{ .Values.service.type }}`).
3. Access the UI via `http://<cluster-ip>:{{ .Values.service.port }}` once the pod is ready.
