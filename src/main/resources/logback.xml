<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} level=%-5level logger=%logger thread=%thread correlation_id=%X{correlation_id:-na} method=%X{method:-na} path=%X{path:-na} msg=%msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
